import stdmem;

func strlen(int, str: []char) {
	asm "	mov rsi, qword [rbp+16]";
	asm "   mov rax, -1";
    asm ".looper:";
    asm "	inc rax";
    asm "	cmp byte [rsi+rax], 0x00";
    asm "	jne .looper";
}

func format([]char, str: []char, format: [][]char) {

}
