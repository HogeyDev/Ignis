[ ] analysis:
    - branch checking: generate warning, not error? idk it feels too risky to blindly prevent compilation here
    - type checking
    - array sizing
    - existence (multipass?)
        - quite broad but im talking about (types, functions, variables, imports, etc...)
