import stdio;
// import stdlib;

struct Person {     // 16 bytes
    name: []char;   // 8 bytes
    age: int;       // 8 bytes
}

func main :: (int) {
    let person: Person = Person { 0 };
    person.name = "Hello, World!";
    println("-----");
    println(person.name);
    println("-----");

    return 0;
}
