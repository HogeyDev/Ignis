import stdio;

func main(int) {
    let sieve: [10]int;

    let i: int = 0;
    while (i < 10) {
        sieve[i] = i;

        i = i + 1;
    }

    let j: int = 0;
    while (j < 10) {
        print_int(j);
        print(": ");
        print_int(sieve[j]);
        println("");

        j = j + 1;
    }
    
    return 0;
}
