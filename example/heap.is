import stdio;
import stdmem;

struct Person {
    name: []char;
    age: int;
}

func main(int) {
    let sizeof_person: usize = <!usize> 16;
    let allocated: @Person = <!@Person> malloc(sizeof_person);

    return 1;
}
